package base;

/**
 * <p>An enum that contains all patterns we are able to identify and verify.</p>
 * <p>If a new pattern has been added, then the enum must be expanded manually.</p>
 */
public enum Pattern {
    SINGLETON
}
